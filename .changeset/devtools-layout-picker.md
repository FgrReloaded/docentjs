---
'@docentjs/devtools': patch
---

Choose the panel's position directly with three layout buttons (left, bottom, right) instead of cycling through them. Each layout remembers its own size. In windows narrower than 640 px the panel docks at the bottom, capped at 60% of the height, and returns to your chosen side when the window is wider.
