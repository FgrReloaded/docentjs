---
title: Arrows, spotlight and overlay
description: Twelve arrow styles, four spotlight shapes, six rings and four overlay styles, all set in the tour JSON.
---

import Demo from '../../../components/Demo.astro'

The default is a small caret, a rounded cutout with a hairline ring, and a dimmed page. Every part can change per tour or per step, in plain JSON, so a builder can author it too.

## Arrows

```ts
options: { arrow: 'curve' }          // for the whole tour
steps: [{ id: 'save', arrow: 'pin' }] // or one step
```

| Style | Looks like |
| --- | --- |
| `caret` | the default notch on the popover's edge |
| `none` | nothing |
| `line`, `dashed`, `dotted` | a straight line with an arrowhead |
| `curve`, `curve-dashed` | a curve that bows outward |
| `squiggle` | a wavy line |
| `loop` | a line with one small loop, like a hand-drawn annotation |
| `elbow` | a right-angle connector with rounded corners |
| `sketch` | a double stroke that reads as hand-drawn |
| `pin` | a dotted line ending in a dot on the target |

Connector styles (everything except `caret` and `none`) place the popover a little further away so the line has room, and draw in after the popover settles. Their code, about 1.8 kB, loads the first time a tour uses one, so tours with the default caret never download it.

<Demo label="Walk through the arrow styles"
  tour={{
    schemaVersion: 1,
    id: 'arrow-gallery',
    options: { showProgress: true },
    steps: [
      { id: 'line', target: { name: 'arrow-target' }, title: 'line', arrow: 'line', placement: 'bottom-start' },
      { id: 'dashed', target: { name: 'arrow-target' }, title: 'dashed', arrow: 'dashed', placement: 'bottom-start' },
      { id: 'dotted', target: { name: 'arrow-target' }, title: 'dotted', arrow: 'dotted', placement: 'bottom-start' },
      { id: 'curve', target: { name: 'arrow-target' }, title: 'curve', arrow: 'curve', placement: 'bottom-start' },
      { id: 'curve-dashed', target: { name: 'arrow-target' }, title: 'curve-dashed', arrow: 'curve-dashed', placement: 'bottom-start' },
      { id: 'squiggle', target: { name: 'arrow-target' }, title: 'squiggle', arrow: 'squiggle', placement: 'bottom-start' },
      { id: 'loop', target: { name: 'arrow-target' }, title: 'loop', arrow: 'loop', placement: 'bottom-start' },
      { id: 'elbow', target: { name: 'arrow-target' }, title: 'elbow', arrow: 'elbow', placement: 'bottom-start' },
      { id: 'sketch', target: { name: 'arrow-target' }, title: 'sketch', arrow: 'sketch', placement: 'bottom-start' },
      { id: 'pin', target: { name: 'arrow-target' }, title: 'pin', arrow: 'pin', placement: 'bottom-start' },
    ],
  }}
>
  <div class="row">
    <button type="button" data-docent="arrow-target">Target</button>
  </div>
</Demo>

## Spotlight

```ts
options: { spotlight: { shape: 'circle', ring: 'pulse', padding: 10 } }
```

| Option | Values |
| --- | --- |
| `shape` | `rounded` (default), `rect`, `pill`, `circle` |
| `ring` | `hairline` (default), `none`, `glow`, `pulse`, `dashed`, `solid` |
| `padding`, `radius` | pixels; radius applies to `rounded` |

`circle` circumscribes the target, so it suits buttons and icons more than wide cards. `pulse` repeats gently to draw the eye and stops for users who prefer reduced motion.

<Demo label="Walk through shapes and rings"
  tour={{
    schemaVersion: 1,
    id: 'spotlight-gallery',
    steps: [
      { id: 'pill', target: { name: 'spot-target' }, title: 'shape: pill', spotlight: { shape: 'pill' } },
      { id: 'circle', target: { name: 'spot-target' }, title: 'shape: circle', spotlight: { shape: 'circle' } },
      { id: 'rect', target: { name: 'spot-target' }, title: 'shape: rect', spotlight: { shape: 'rect' } },
      { id: 'glow', target: { name: 'spot-target' }, title: 'ring: glow', spotlight: { ring: 'glow' } },
      { id: 'pulse', target: { name: 'spot-target' }, title: 'ring: pulse', spotlight: { ring: 'pulse' } },
      { id: 'dashed', target: { name: 'spot-target' }, title: 'ring: dashed', spotlight: { ring: 'dashed' } },
      { id: 'solid', target: { name: 'spot-target' }, title: 'ring: solid', spotlight: { ring: 'solid' } },
    ],
  }}
>
  <div class="row">
    <button type="button" data-docent="spot-target">Spotlight me</button>
  </div>
</Demo>

## Overlay

```ts
options: { overlay: { style: 'blur', blur: 6, opacity: 0.4 } }
```

| Style | Effect |
| --- | --- |
| `dim` | a tinted scrim (default) |
| `blur` | the scrim plus a soft blur of the page |
| `vignette` | clear near the target, darker toward the edges |
| `none` | no scrim, and the page stays usable, for lighter hint-style tours |

`color` and `opacity` tint the scrim; `blur` sets the blur radius. With `none`, connectors and the ring switch to the accent color so they stay visible on the page.

<Demo label="Walk through overlay styles"
  tour={{
    schemaVersion: 1,
    id: 'overlay-gallery',
    steps: [
      { id: 'blur', target: { name: 'overlay-target' }, title: 'overlay: blur', overlay: { style: 'blur' } },
      { id: 'vignette', target: { name: 'overlay-target' }, title: 'overlay: vignette', overlay: { style: 'vignette' } },
      { id: 'none', target: { name: 'overlay-target' }, title: 'overlay: none', body: 'The page stays usable.', overlay: { style: 'none' }, arrow: 'curve' },
    ],
  }}
>
  <div class="row">
    <button type="button" data-docent="overlay-target">Target</button>
  </div>
</Demo>

## Where to set it

From lowest to highest precedence: renderer options (`createTour(tour, { renderer: { arrow, spotlight, overlay } })`), a [template](/customize/slots-and-templates/), the tour's `options`, then the step. Colors come from the `connector` and `ring` theme tokens.

The [devtools](/guides/devtools/) Edit tab switches all of these live on a running tour.
