<!-- Thanks for the pull request. CONTRIBUTING.md has the setup and the checks. -->

## What changed

<!-- And why. Link the issue it closes, if there is one. -->

## Checks

- [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` and `pnpm test:e2e` pass
- [ ] A test covers the change (a bug fix has the test that failed without it)
- [ ] Docs in `apps/docs` updated, if behaviour or options changed
- [ ] `pnpm changeset` added, if a published package changed
